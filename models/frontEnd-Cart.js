import mongoose from 'mongoose';

const feCartSchema = new mongoose.Schema(
	{
		//product id - ref to product -
		//variant id - ref to product - Send all variants for product.
		//proudct qunt - number
	}
);
